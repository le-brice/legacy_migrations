version: 2

sources:
  - name: raw
    description: Raw seeded tables used by the migrated legacy chain.
    database: dbt_sandbox_brice
    schema: RAW
    tables:
      - name: customers
        description: Raw customer input records.
      - name: orders
        description: Raw order input records.
